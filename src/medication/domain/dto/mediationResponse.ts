export default interface MedicationResponse {
    id: number;
    name: string;
    category: string;
    form: string;
    strength: string;
    classification: string;
    indication: string;
    text: string
}